coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch:
      default:
        target: 80%
        threshold: 10%

ignore:
  - "tests/**"
  - "examples/**"
  - "**/mod.rs"  # Ignore test modules in mod.rs files
